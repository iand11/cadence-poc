// Compatibility re-export — favorites now live in a context so every consumer
// shares one per-user list. See src/context/FavoritesContext.jsx.
export { useFavorites } from '../context/FavoritesContext';
