// Compatibility re-export — the tracked roster now lives in a context so the
// whole app shares one fetch. See src/context/TrackedArtistsContext.jsx.
export { useTrackedArtists } from '../context/TrackedArtistsContext';
